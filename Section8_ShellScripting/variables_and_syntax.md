# Variables and Basic Syntax in Shell Scripting

- Variables are assigned without spaces: `name="value"`.
- Access variable value with `$name`.
- Arithmetic operations use `$(())`, e.g., `sum=$((num1+num2))`.
- Use `echo` to print variables.
